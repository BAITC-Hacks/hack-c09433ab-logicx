# Copy this file to .env next to package.json.
# The reviewer supplies their own OpenAI API key. Never commit the real key.
OPENAI_API_KEY=

# Optional: use a model available to your OpenAI project with Responses API
# and Structured Outputs support. Default used by the app:
OPENAI_MODEL=gpt-4o-mini
